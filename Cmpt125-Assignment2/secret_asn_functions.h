// secret_asn_functions.c

//
// Provide your own implementations of these functions to help develop your
// testing functions in testing_asn.h.
//
// The implementations given here are dummy implementations that don't do
// anything sensible. They are written so they compile with the course
// makeifle.
//
// See testing_asn.h for the pre/post conditions for these functions.
//

int sum_arr1(int* arr, int n) {
    arr[n-1] = arr[n-1];
    return -1;
}

int sum_arr2(int* arr, int n) {
    arr[n-1] = arr[n-1];
    return -1;
}

int sum_arr3(int* arr, int n) {
    arr[n-1] = arr[n-1];
    return -1;
}

int sum_arr4(int* arr, int n) {
    arr[n-1] = arr[n-1];
    return -1;
}


////////////////////////////////////////////////////////////////////////////


int binary_search1(int* v, int n, int x) {
    v[n-1] += x - x ;
    return -1;
}

int binary_search2(int* v, int n, int x) {
    v[n-1] += x - x ;
    return -1;
}


////////////////////////////////////////////////////////////////////////////


void sort1(int* v, int n) {
    v[n-1] = v[n-1];
}

void sort2(int* v, int n) {
    v[n-1] = v[n-1];
}
